# Synthetic robot example

`robot.toml` demonstrates subject-neutral extraction configuration. No artwork is
committed: tests generate small geometric robot-like fixtures in temporary directories,
so provenance and redistribution are unambiguous.
